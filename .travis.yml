language: node_js

node_js: 
  - 9
  - node

install:
  - npm install -g codecov

before_script: 
  - chmod 0777 ./node_modules/.bin/istanbul

script:
  - "npm start &"
  - "npm run test-cover"
  
after_success:
    - codecov
